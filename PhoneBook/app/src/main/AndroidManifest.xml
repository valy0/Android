<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.valiocholakov.phonebook">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.ContactsActivity"
            android:label="@string/contacts"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.ContactDetailsActivity"
            android:label="@string/contact_details" />
        <activity
            android:name=".activities.AddContactActivity"
            android:label="@string/add_contact" />
        <activity android:name=".activities.BaseActivity" />
        <activity
            android:name=".activities.CountiesActivity"
            android:label="@string/counties">
        </activity>
    </application>

</manifest>